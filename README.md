# Astro Navbar Styling Test 2

Make sure that you have the latest nodejs version installed to run Astro.

Clone the repo then run

```sh
npm install
```
to install Astro, Tailwind CSS, Astro-Navbar component 

```sh
npm run dev  
```
to run a small dev server that lives at ```localhost:4321``` 
